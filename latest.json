{
  "version": "1.6.5",
  "url": "https://github.com/motaz-hefny/Distrib_Repo/releases/latest",
  "notes": "Linux start menu icon missing: — electron-builder icon path was `build/icon.png` but `buildResources` directory is `build`, making the effective path `build/build/icon.png` (non-existent). Fixed to `icon.png` (relative to buildResources). | Source code published to public Distrib_Repo: — removed the `git archive` step and `dist/source/source.tar.gz` from release workflow assets. The public repo now only receives binaries (`.AppImage`, `.deb`, `.tar.gz`). Note: GitHub auto-generates `Source code (zip)` and `Source code (tar.gz)` for every release and cannot be disabled. | `npm run build:local` script: — builds the app and packages only the `.deb` target locally (`electron-builder --linux deb --publish never`), so you can install without redownloading from GitHub."
}
